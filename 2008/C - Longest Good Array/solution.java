import java.util.Scanner; public class homework {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();                for (int i = 0; i < t; i++) {            long l = sc.nextLong();            long r = sc.nextLong();                        // Binary search for the maximum k such that l + k*(k-1)/2 <= r            long left = 1;            long right = 2; // Start with a reasonable upper bound                        // First, find an upper bound for k            while (l + right * (right - 1) / 2 <= r) {                right *= 2;                if (right > 2e5) break; // Safety check since k won't be too large            }                        // Now binary search between left and right            long maxK = 0;            while (left <= right) {                long mid = left + (right - left) / 2;                long sum = mid * (mid - 1) / 2;                if (l + sum <= r) {                    maxK = mid;                    left = mid + 1;                } else {                    right = mid - 1;                }            }                        System.out.println(maxK);        }    }}